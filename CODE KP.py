"""
Контрольная работа по алгоритмам сортировки и поиска
Все алгоритмы и результаты их работы в одном файле
"""

print("=" * 60)
print("КОНТРОЛЬНАЯ РАБОТА ПО АЛГОРИТМАМ")
print("=" * 60)

# =============================================================================
# АЛГОРИТМЫ СОРТИРОВКИ
# =============================================================================

print("\n" + "=" * 50)
print("АЛГОРИТМЫ СОРТИРОВКИ")
print("=" * 50)

# -----------------------------------------------------------------------------
# 1. БЛОЧНАЯ (КОРЗИННАЯ) СОРТИРОВКА
# -----------------------------------------------------------------------------
def bucket_sort(arr):
    """
    Блочная сортировка - распределяет элементы по корзинам
    """
    if len(arr) == 0:
        return arr
    
    # Создаём пустые корзины
    num_buckets = len(arr)
    buckets = [[] for _ in range(num_buckets)]
    
    # Распределяем элементы по корзинам
    for num in arr:
        index = int(num * num_buckets)  # Для чисел в диапазоне [0, 1)
        buckets[index].append(num)
    
    # Сортируем каждую корзину
    for bucket in buckets:
        bucket.sort()
    
    # Объединяем корзины в исходный массив
    k = 0
    for bucket in buckets:
        for num in bucket:
            arr[k] = num
            k += 1
    return arr

print("\n--- Блочная сортировка (Bucket Sort) ---")
arr1 = [0.42, 0.32, 0.33, 0.52, 0.37, 0.47, 0.51]
print(f"Исходный массив: {arr1}")
result1 = bucket_sort(arr1.copy())
print(f"Отсортированный массив: {result1}")

# -----------------------------------------------------------------------------
# 2. БЛИННАЯ СОРТИРОВКА
# -----------------------------------------------------------------------------
def pancake_sort(arr):
    """
    Блинная сортировка - использует перевороты частей массива
    """
    n = len(arr)
    for i in range(n, 0, -1):
        # Находим индекс максимального элемента в неотсортированной части
        max_index = arr.index(max(arr[:i]))
        
        # Если максимум не на своём месте, переворачиваем
        if max_index != i - 1:
            # Переворачиваем до максимума
            arr = arr[:max_index+1][::-1] + arr[max_index+1:]
            # Переворачиваем всю неотсортированную часть
            arr = arr[:i][::-1] + arr[i:]
    return arr

print("\n--- Блинная сортировка (Pancake Sort) ---")
arr2 = [3, 1, 4, 2, 5]
print(f"Исходный массив: {arr2}")
result2 = pancake_sort(arr2.copy())
print(f"Отсортированный массив: {result2}")

# -----------------------------------------------------------------------------
# 3. СОРТИРОВКА БУСИНАМИ (ГРАВИТАЦИОННАЯ)
# -----------------------------------------------------------------------------
def bead_sort(arr):
    """
    Сортировка бусинами - моделирует падение бусин под гравитацией
    """
    if not arr:
        return []
    
    # Определяем максимальное значение
    max_val = max(arr)
    
    # Создаём матрицу бусин
    beads = [[0] * max_val for _ in range(len(arr))]
    
    # Расставляем бусины
    for i, num in enumerate(arr):
        for j in range(num):
            beads[i][j] = 1
    
    # Гравитация: бусины падают вниз
    for j in range(max_val):
        sum_col = sum(beads[i][j] for i in range(len(arr)))
        for i in range(len(arr)):
            beads[i][j] = 1 if i < sum_col else 0
    
    # Считываем отсортированные значения
    for i in range(len(arr)):
        arr[i] = sum(beads[i])
    
    return arr

print("\n--- Сортировка бусинами (Bead Sort) ---")
arr3 = [3, 1, 4, 2]
print(f"Исходный массив: {arr3}")
result3 = bead_sort(arr3.copy())
print(f"Отсортированный массив: {result3}")

# =============================================================================
# АЛГОРИТМЫ ПОИСКА
# =============================================================================

print("\n" + "=" * 50)
print("АЛГОРИТМЫ ПОИСКА")
print("=" * 50)

# -----------------------------------------------------------------------------
# 4. ПОИСК СКАЧКАМИ
# -----------------------------------------------------------------------------
import math

def jump_search(arr, x):
    """
    Поиск скачками - прыгает через √n элементов
    """
    n = len(arr)
    if n == 0:
        return -1
    
    step = int(math.sqrt(n))
    prev = 0
    
    # Прыжки вперёд
    while prev < n and arr[min(step, n) - 1] < x:
        prev = step
        step += int(math.sqrt(n))
        if prev >= n:
            return -1
    
    # Линейный поиск в блоке
    for i in range(prev, min(step, n)):
        if arr[i] == x:
            return i
    return -1

print("\n--- Поиск скачками (Jump Search) ---")
arr4 = [1, 3, 5, 7, 9, 11, 13, 15, 17]
x4 = 9
print(f"Массив: {arr4}")
print(f"Ищем элемент: {x4}")
result4 = jump_search(arr4, x4)
print(f"Элемент найден на позиции: {result4}")

# -----------------------------------------------------------------------------
# 5. ЭКСПОНЕНЦИАЛЬНЫЙ ПОИСК
# -----------------------------------------------------------------------------
def exponential_search(arr, x):
    """
    Экспоненциальный поиск - сочетает экспоненциальное расширение и бинарный поиск
    """
    n = len(arr)
    if n == 0:
        return -1
    
    if arr[0] == x:
        return 0
    
    i = 1
    # Экспоненциальное увеличение диапазона
    while i < n and arr[i] <= x:
        i *= 2
    
    # Бинарный поиск в диапазоне [i/2, min(i, n)]
    low = i // 2
    high = min(i, n - 1)
    
    while low <= high:
        mid = (low + high) // 2
        if arr[mid] == x:
            return mid
        elif arr[mid] < x:
            low = mid + 1
        else:
            high = mid - 1
    return -1

print("\n--- Экспоненциальный поиск (Exponential Search) ---")
arr5 = [1, 3, 5, 7, 9, 11, 13, 15, 17]
x5 = 11
print(f"Массив: {arr5}")
print(f"Ищем элемент: {x5}")
result5 = exponential_search(arr5, x5)
print(f"Элемент найден на позиции: {result5}")

# -----------------------------------------------------------------------------
# 6. ТЕРНАРНЫЙ ПОИСК
# -----------------------------------------------------------------------------
def ternary_search(arr, l, r, x):
    """
    Тернарный поиск - делит массив на три части
    """
    if r >= l:
        mid1 = l + (r - l) // 3
        mid2 = r - (r - l) // 3
        
        if arr[mid1] == x:
            return mid1
        if arr[mid2] == x:
            return mid2
        
        if x < arr[mid1]:
            return ternary_search(arr, l, mid1 - 1, x)
        elif x > arr[mid2]:
            return ternary_search(arr, mid2 + 1, r, x)
        else:
            return ternary_search(arr, mid1 + 1, mid2 - 1, x)
    return -1

print("\n--- Тернарный поиск (Ternary Search) ---")
arr6 = [1, 3, 5, 7, 9, 11, 13, 15, 17]
x6 = 7
print(f"Массив: {arr6}")
print(f"Ищем элемент: {x6}")
result6 = ternary_search(arr6, 0, len(arr6) - 1, x6)
print(f"Элемент найден на позиции: {result6}")

# =============================================================================
# РЕЗУЛЬТАТЫ РАБОТЫ ВСЕХ АЛГОРИТМОВ
# =============================================================================

print("\n" + "=" * 60)
print("СВОДНЫЕ РЕЗУЛЬТАТЫ РАБОТЫ АЛГОРИТМОВ")
print("=" * 60)

print("\n✅ Все алгоритмы успешно выполнены!")
print("📊 Результаты сортировки:")
print(f"   • Блочная сортировка: {result1}")
print(f"   • Блинная сортировка: {result2}")
print(f"   • Сортировка бусинами: {result3}")

print("\n🔍 Результаты поиска:")
print(f"   • Поиск скачками: элемент 9 на позиции {result4}")
print(f"   • Экспоненциальный поиск: элемент 11 на позиции {result5}")
print(f"   • Тернарный поиск: элемент 7 на позиции {result6}")

print("\n" + "=" * 60)
print("ВРЕМЕННАЯ СЛОЖНОСТЬ АЛГОРИТМОВ")
print("=" * 60)

print("""
СОРТИРОВКА:
• Блочная сортировка: O(n + k)
• Блинная сортировка: O(n²)
• Сортировка бусинами: O(n + S)

ПОИСК:
• Поиск скачками: O(√n)
• Экспоненциальный поиск: O(log n)
• Тернарный поиск: O(log₃ n)
""")

print("🎯 Контрольная работа завершена успешно!")
